[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = [
  "cd apps/backend",
  "npm install"
]

[phases.build]
cmds = [
  "cd apps/backend && npm run build"
]

[start]
cmd = "cd apps/backend && npm start"

